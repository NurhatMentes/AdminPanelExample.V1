@model AdminPanelV1.Models.ProductSliders

@{
    ViewBag.Title = "Ürün Slider Sil";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h3>Silmek istediğinize emin misiniz?</h3>
<div class="card alert-danger">
    <div class="card-body">
        <h4 class="card-title">Ürün Slider</h4>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Products.Title)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Products.Title)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ImgUrl)
            </dt>

            <dd>
                @*@Html.DisplayFor(model => model.ImgUrl)*@
                <img src="@Model.ImgUrl" style="width:180px; height:150px" />
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Sil" class="btn btn-danger" /> |
                @Html.ActionLink("Geri Dön", "Index")
            </div>}
    </div>
</div>